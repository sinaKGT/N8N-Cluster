services:
  n8n-demo:
    image: n8nio/n8n:latest
    container_name: n8n-demo
    restart: always
    environment:
      - N8N_PORT=5678
      - N8N_SECURE_COOKIE=false
      - WEBHOOK_URL=http://demo.localhost/
      - N8N_EDITOR_BASE_URL=http://demo.localhost/
    volumes:
      - ../../volumes/n8n-demo:/home/node/.n8n
