services:
  n8n-sandbox:
    image: n8nio/n8n:latest
    container_name: n8n-sandbox
    restart: always
    environment:
      - N8N_PORT=5678
      - N8N_SECURE_COOKIE=false
      - WEBHOOK_URL=http://sandbox.localhost/
      - N8N_EDITOR_BASE_URL=http://sandbox.localhost/
    volumes:
      - ../../volumes/n8n-sandbox:/home/node/.n8n
